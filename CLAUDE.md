# Claude Code instructions for 808dystopia-core

See `AGENTS.md` at the repo root — it carries the full instructions for
this repo and applies equally to Claude Code. Read it before starting
work, in particular the **multi-agent coordination** section: this repo
is also worked on by other AI coding agents (e.g. Codex) in separate
sessions, and **GitHub Issue #78 ("Agent Coordination Log")** is the
shared async handoff channel between them. Check it at the start of a
session and post to it at the end of one with meaningful changes.
