# Claude Code instructions for 808dystopia-core

See `AGENTS.md` at the repo root — it carries the full instructions for
this repo. Read it before starting work, in particular the **session
handoff log** section: separate Claude Code sessions share no memory, and
**GitHub Issue #78 ("Agent Coordination Log")** is the async handoff
channel between them. Check it at the start of a session and post to it
at the end of one with meaningful changes.
