// Step 7: publish the carousel (mixed image+video slides) via Composio's
// Instagram tools, then post the hashtag block as a separate first comment.
// Gated behind config.publish (CAROUSEL_PUBLISH=1) so nothing posts until
// the pipeline is actually built and tested.
// TODO: implement once Composio's Instagram connection + an image/video
// hosting approach are decided.

export async function publishCarousel(_input) {
  throw new Error("not implemented: publishCarousel");
}
