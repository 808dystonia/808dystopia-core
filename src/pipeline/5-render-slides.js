// Step 5: render the carousel slides (HTML/CSS -> PNG via Puppeteer) using
// the templates in src/templates/. Slide 1 is the cover; slide 2 depends on
// classification (tracklist / diss lyric / general context). The final
// slide is the pre-made closer video, reused as-is (not rendered here).
// TODO: implement once the Canva-modeled templates and font are in place.

export async function renderSlides(_input) {
  throw new Error("not implemented: renderSlides");
}
