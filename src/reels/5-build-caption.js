// Step 5: build the Reel caption — factual, to-the-point, same tone as
// the news carousel's caption (src/pipeline/6-build-caption.js), no
// separate stylized voice guide.
// TODO: implement alongside the other steps — likely a much simpler
// version of the news carousel's caption builder, since there's no
// per-type branching (album_drop/diss/other) here, just one video per day.
export async function buildReelCaption(_input) {
  throw new Error("not implemented: buildReelCaption");
}
